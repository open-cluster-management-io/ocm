kind: Endpoints
apiVersion: v1
metadata:
  name: cluster-manager-work-webhook
  namespace: {{ .ClusterManagerNamespace }}
  labels:
    {{ if gt (len .Labels) 0 }}
    {{ range $key, $value := .Labels }}
    "{{ $key }}": "{{ $value }}"
    {{ end }}
    {{ end }}
subsets:
  - addresses:
      - ip: {{.WorkWebhook.HostedAddress}}
    ports:
      - port: {{.WorkWebhook.HostedPort}}
        name: tls
